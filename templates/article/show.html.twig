{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.title }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ article.prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ article.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('article_index') }}"> <button class="btn btn-secondary">back to list</a></button>

    <a href="{{ path('article_edit', {'id': article.id}) }}"> <button class="btn btn-warning">edit</a></button>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
